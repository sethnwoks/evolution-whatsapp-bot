services:
  # 1. Evolution API Engine - THE HEART
  - type: web
    name: evo-engine
    runtime: image
    image:
      url: docker.io/evoapicloud/evolution-api:latest
    plan: free
    region: frankfurt
    envVars:
      - key: PORT
        value: "10000"
      - key: SERVER_PORT
        value: "10000"
      - key: AUTHENTICATION_STORE_TYPE
        value: database
      - key: DATABASE_ENABLED
        value: "true"
      - key: DATABASE_PROVIDER
        value: postgresql
      - key: DATABASE_CONNECTION_URI
        sync: false
      - key: CACHE_REDIS_ENABLED
        value: "true"
      - key: CACHE_REDIS_URI
        sync: false
      - key: AUTHENTICATION_API_KEY
        generateValue: true

  # 2. Python Bot Brain - THE LOGIC
  - type: web
    name: evo-bot
    runtime: docker
    dockerContext: ./bot
    dockerfilePath: bot/Dockerfile
    plan: free
    region: frankfurt
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "8000" # MATCHED: Adjusted to match your Python code's default
      - key: SERVER_URL
        fromService:
          name: evo-engine
          type: web
          property: host
      - key: AUTHENTICATION_API_KEY
        fromService:
          name: evo-engine
          type: web
          envVarKey: AUTHENTICATION_API_KEY
      - key: INSTANCE_NAME
        value: fire
