# Render Blueprint for Evolution API + Python Bot
# This file defines the orchestration between the engine and the brain.
services:
  # 1. Evolution API - The Core Engine
  - type: web
    name: evolution-api
    runtime: image
    image:
      url: docker.io/evoapicloud/evolution-api:latest
    plan: free
    region: frankfurt
    # NO healthCheckPath for API - Allows it to finish migrations on its own time
    envVars:
      - key: PORT
        value: "8080"
      - key: SERVER_PORT
        value: "8080"
      - key: DATABASE_CONNECTION_CLIENT_NAME
        value: evolution_production # Fresh ID to trigger re-sync
      - key: AUTHENTICATION_STORE_TYPE
        value: database
      - key: DATABASE_ENABLED
        value: "true"
      - key: DATABASE_PROVIDER
        value: postgresql
      - key: DATABASE_CONNECTION_URI
        sync: false
      - key: CACHE_REDIS_ENABLED
        value: "true"
      - key: CACHE_REDIS_URI
        sync: false
      - key: AUTHENTICATION_API_KEY
        generateValue: true

  # 2. Python Bot Brain - Your logic
  - type: web
    name: evolution-bot
    runtime: docker
    rootDir: ./bot
    plan: free
    healthCheckPath: /health # Keeping this as it confirmed the bot is healthy
    envVars:
      - key: PORT
        value: "8000"
      - key: SERVER_URL
        fromService:
          name: evolution-api
          type: web
          property: host
      - key: AUTHENTICATION_API_KEY
        fromService:
          name: evolution-api
          type: web
          envVarKey: AUTHENTICATION_API_KEY
      - key: INSTANCE_NAME
        value: fire
